#!/usr/bin/node
const req = require ('request');
const url = '${process.argv[2]}18';

req.get(url, {decoding: 'utf-8'})
const response = JSON.parse(data);
.on('data', function (data) {
    if (response.characters === "https://swapi-api.alx-tools.com/api/people/18/"}
};

